---
title: "Welcome to My Blog"
date: "2026-01-25"
excerpt: "Introducing my new high-performance blog built with Next.js 16, featuring neo-brutalist design and advanced code highlighting."
slug: "welcome-to-my-blog"
tags: ["nextjs", "web-development", "performance"]
thumbnail: "/images/posts/welcome-to-my-blog/thumbnail.jpg"
hero: "/images/posts/welcome-to-my-blog/hero.jpg"
published: true
author: "Dakota Smith"
keywords: ["Next.js", "blog", "web development", "neo-brutalist design"]
---

# Welcome to My Blog

This is my new personal blog, built with cutting-edge web technologies and designed for maximum performance and accessibility.

## Tech Stack

The blog is powered by:

- **Next.js 16+** with App Router for blazing-fast static site generation
- **TypeScript** for type-safe development
- **Tailwind CSS** with a custom neo-brutalist dark theme
- **MDX** for rich content authoring with React components

## Performance First

Every aspect of this blog is optimized for speed:

```typescript {3,7-14}
// Example: Optimized image loading
import Image from 'next/image';

export function HeroImage({ src, alt }: { src: string; alt: string }) {
  return (
    <Image
      src={src}
      alt={alt}
      width={1600}
      height={900}
      priority
      placeholder="blur"
      className="w-full h-auto"
    />
  );
}
```

### Advanced Code Features

This blog supports advanced code highlighting features:

**Line Highlighting** - Specific lines can be highlighted:

```javascript {1,3-5}
function fibonacci(n) {
  if (n <= 1) return n;
  return fibonacci(n - 1) + fibonacci(n - 2);
}

console.log(fibonacci(10)); // 55
```

**Diff Mode** - Show code changes with visual indicators:

```typescript diff
- const oldValue = 'deprecated';
+ const newValue = 'modern approach';

  function updateConfig() {
-   return { legacy: true };
+   return { modern: true, performance: 'optimized' };
  }
```

**Multiple Languages** - Syntax highlighting for 20+ languages:

```python
def calculate_performance_score(metrics):
    """Calculate Lighthouse performance score."""
    weights = {
        'fcp': 0.10,
        'lcp': 0.25,
        'tbt': 0.30,
        'cls': 0.25,
        'si': 0.10
    }

    score = sum(metrics[key] * weights[key] for key in weights)
    return round(score * 100, 2)
```

```rust
fn optimize_bundle_size(modules: &[Module]) -> Vec<Module> {
    modules
        .iter()
        .filter(|m| m.size < MAX_MODULE_SIZE)
        .map(|m| m.tree_shake())
        .collect()
}
```

## What's Next

In upcoming posts, I'll be sharing:

- Advanced Next.js optimization techniques
- Building accessible web applications
- Deep dives into modern JavaScript frameworks
- Hardware and AI project updates

Stay tuned for weekly tech content!
